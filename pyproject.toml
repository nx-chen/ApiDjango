[tool.poetry]
name = "ApiDjango"
version = "1.0.0"
description = ""
authors = ["Ningxi Chen <ningxi.chen@epita.fr>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
